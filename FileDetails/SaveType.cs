namespace FileDetails
{
    public enum SaveType
    {
        /// <summary>
        /// Save the data as text file
        /// </summary>
        Text,
        /// <summary>
        /// Save the data as markdown file
        /// </summary>
        Markdown
    }
}
